{
  "rules": {
    "orders": {
      ".read": true,
      ".write": true,
      "$orderId": {
        ".validate": "newData.exists()"
      }
    },
    "baristas": {
      ".read": true,
      ".write": true,
      "$baristaId": {
        ".validate": "newData.exists()"
      }
    }
  }
}
